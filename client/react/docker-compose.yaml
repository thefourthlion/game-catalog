version: "25.0.2"
services:
  client:
    build: /
    container_name: thebombroms-client
    ports:
      - '4002:4002'
    # wont work in windows
    # volumes:
    #   - ./client/react:/thebombroms
    stdin_open: true
    tty: true
#     networks:
#       - docker-network
# networks:
#   docker-network:
#     external: true
